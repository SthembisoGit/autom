# Archived Oracle Notes

The Oracle VM path is no longer the active deployment route for `autoM`.

Use [docs/deployment/production-setup/README.md](production-setup/README.md)
for the current Funnel-based personal deployment guide.

Keep this file only as a historical note so the old Oracle references do not
lead you down the wrong path again.
